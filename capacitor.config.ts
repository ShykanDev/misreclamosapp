import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Mis Reclamos',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
